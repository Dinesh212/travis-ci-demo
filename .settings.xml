<?xml version="1.0"?>
<settings>
    <servers> 
        <server>
            <id>anypoint-platform</id>
            <username>${env.username}</username>
            <password>${env.password}</password>
        </server>
    </servers>
</settings>